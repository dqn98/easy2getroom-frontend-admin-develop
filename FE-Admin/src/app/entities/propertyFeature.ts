import { Feature } from './feature';
import { Property } from './property';

export class PropertyFeature {
    featureId: number;
    proeprtyId: number;
    feature: Feature;
    property: Property;
}